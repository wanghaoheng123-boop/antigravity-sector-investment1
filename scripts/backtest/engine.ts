/**
 * Canonical backtest engine: `lib/backtest/engine.ts`.
 * This file re-exports it so any legacy imports from `scripts/backtest/engine` stay aligned.
 */
export * from '../../lib/backtest/engine'
